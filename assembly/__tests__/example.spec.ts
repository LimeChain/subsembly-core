import { Header } from "../models";
import { TestHeader } from "../models/test-header";
describe("example", () => {
  it("should intstantiate new header", () => {
    const header = new Header();
  })
});
